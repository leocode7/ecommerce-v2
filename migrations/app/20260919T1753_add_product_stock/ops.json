[
  {
    "id": "column.public.product.stock",
    "label": "Add column \"stock\" to \"product\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "stock",
        "table": "product"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"stock\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "product",
          "stock"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"stock\"",
        "sql": "ALTER TABLE \"public\".\"product\" ADD COLUMN \"stock\" int4 DEFAULT 0 NOT NULL"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"stock\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "product",
          "stock"
        ]
      }
    ]
  }
]