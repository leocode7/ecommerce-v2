[
  {
    "id": "dropColumn.product.peso",
    "label": "Drop column \"peso\" from \"product\"",
    "operationClass": "destructive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "peso",
        "table": "product"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"peso\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "product",
          "peso"
        ]
      }
    ],
    "execute": [
      {
        "description": "drop column \"peso\"",
        "sql": "ALTER TABLE \"public\".\"product\" DROP COLUMN \"peso\""
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"peso\" does not exist",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "product",
          "peso"
        ]
      }
    ]
  }
]